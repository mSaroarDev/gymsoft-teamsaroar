import { H1 } from "@/subcomponents/Headings";
import Loading from "@/subcomponents/Loading";

const page = () => {
    return (
        <>
            <Loading />

            <H1 text={"this si demo"} />
        </>
    );
};

export default page;